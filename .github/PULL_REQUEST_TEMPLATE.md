## What does this change?

<!-- Summarize the change and why it's needed. -->

## How was this tested?

<!-- e.g. "ran npm run build:api / build:web", "exercised via the bots
smoke script", "manually verified in two browser windows" -->

## Checklist

- [ ] `npm run build:api` and `npm run build:web` both pass locally
- [ ] Changes to `packages/shared` are reflected in both `apps/api` and
      `apps/web` where consumed
- [ ] No secrets or `.env` files committed
- [ ] If this touches money, wagering, or personal data: flagged for
      review against `DISCLAIMER.md` / the legal document templates
